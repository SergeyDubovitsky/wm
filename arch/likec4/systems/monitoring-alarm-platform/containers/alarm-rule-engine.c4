model {
    extend monitoring-alarm-platform {
        alarm-rule-engine = container 'Alarm Rule Engine' {
            description 'Обрабатывает входящие события по правилам, ведет жизненный цикл alarm и инициирует уведомления и эскалации.'
            technology 'Rule engine / stream processor'
            icon bootstrap:alarm

            -> kafka-event-log 'Читает Kafka topics с telemetry events и derived events'
            -> telemetry-store 'Читает аналитический контекст и записывает immutable alarm history'
            -> platform-store 'Записывает current alarm state, acknowledgements, mutes и workflow state'
            -> notification-service 'Запускает доставку уведомлений'
        }
    }
}
