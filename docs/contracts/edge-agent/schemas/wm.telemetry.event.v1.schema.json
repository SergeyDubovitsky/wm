{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://industrial-edge-web-monitoring.local/contracts/edge-agent/wm.telemetry.event.v1.schema.json",
    "title": "wm.telemetry.event.v1",
    "description": "Thin MQTT payload для telemetry event; routing identity берется из MQTT topic path.",
    "type": "object",
    "additionalProperties": false,
    "required": [
        "message_type",
        "event_id",
        "event_type",
        "catalog_revision",
        "ts",
        "observation_mode",
        "value",
        "quality",
        "sequence"
    ],
    "properties": {
        "message_type": {
            "const": "wm.telemetry.event.v1"
        },
        "event_id": {
            "type": "string",
            "minLength": 1,
            "description": "Непрозрачная непустая строка для дедупликации; не UUID-only."
        },
        "event_type": {
            "type": "string",
            "enum": [
                "telemetry.changed",
                "telemetry.sample"
            ]
        },
        "catalog_revision": {
            "type": "string",
            "minLength": 1,
            "description": "Версия retained source metadata catalog, использованная при формировании события."
        },
        "ts": {
            "type": "string",
            "format": "date-time"
        },
        "observation_mode": {
            "type": "string",
            "enum": [
                "listen",
                "read_on_start",
                "periodic_read"
            ]
        },
        "value": {
            "type": [
                "boolean",
                "number",
                "string",
                "null"
            ]
        },
        "value_raw": {
            "type": [
                "string",
                "null"
            ]
        },
        "quality": {
            "type": "string",
            "enum": [
                "good",
                "uncertain",
                "bad"
            ]
        },
        "sequence": {
            "type": "integer",
            "minimum": 1
        }
    }
}
