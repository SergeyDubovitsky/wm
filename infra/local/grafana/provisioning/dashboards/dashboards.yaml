apiVersion: 1

providers:
  - name: web-monitoring-local
    orgId: 1
    folder: Local
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: $GRAFANA_DASHBOARDS_PATH
